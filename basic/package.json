{
  "name": "nodedk-template",
  "version": "0.1.0",
  "description": "NodeDK template",
  "scripts": {
    "dev": "nodemon -e '*' -i dist -x ndk serve",
    "test": "spec",
    "test:watch": "nodemon --exec spec",
    "deploy": "ndk deploy",
    "serve": "ndk serve",
    "build": "ndk build",
    "sitemap": "ndk sitemap",
    "generate": "ndk generate",
    "cmd": "ndk cmd",
    "migrate": "ndk migrate",
    "locales": "ndk locales",
    "install": "node install.js"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/nodedk/nodedk-template"
  },
  "author": "",
  "dependencies": {
    "@nodedk/app": "^0.1.0"
  }
}
